/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details on building Java & JVM projects, please refer to https://docs.gradle.org/8.12.1/userguide/building_java_projects.html in the Gradle documentation.
 * This project uses @Incubating APIs which are subject to change.
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // This dependency is used by the application.
    implementation libs.guava
}

testing {
    suites {
        // Configure the built-in test suite
        test {
            // Use JUnit4 test framework
            useJUnit('4.13.2')
        }
    }
}

// Apply a specific Java toolchain to ease working on different environments.
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

application {
    // Define the main class for the application.
    mainClass = 'az.edu.ada.wm2.App'
}

task myPrepare {
    group 'my-funny-operations'
    description 'Summoning the build spirits...'
    doLast {
        println '🔮 Chanting spells and preparing the environment...'
    }
}

task myCompileCode(dependsOn: myPrepare) {
    group 'my-funny-operations'
    description 'Casting compile magic...'
    doLast {
        println '✨ Abracadabra! Turning source into magic code...'
    }
}

task myPackageApp(dependsOn: myCompileCode) {
    group 'my-funny-operations'
    description 'Stuffing code into an enchanted box...'
    doLast {
        println '📦 Wrapping the app like a holiday present...'
    }
}

task myDeploy(dependsOn: myPackageApp) {
    group 'my-funny-operations'
    description 'Launching the magic into the wild...'
    doLast {
        println '🚀 Deploying with style! Off it goes into the great beyond...'
    }
}